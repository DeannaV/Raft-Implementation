import { StatusType, ServerState, ServerConfig } from "../types";

export async function candidate(serverConfig: ServerConfig, serverState: ServerState, numberOfNodes: number): Promise<ServerState> {

    return serverState;
}